{% extends "base.html.twig" %}

{% if app.user %}
    {% set username = app.user.username %}
{% else %}
    {% set username = '' %}
{% endif %}

{% block body %}
    {# React component #}
    <div id="ticket-component" data-username="{{ username }}" data-ticket-id="{{ ticket_id }}"></div>
{% endblock %}
